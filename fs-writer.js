const fs = require('fs')

// write to file - create before write
// fs.writeFileSync('resources/notes.txt', 'File created using node fs')

// append to existing file or create if file not exists
// fs.appendFileSync('resources/notes1.txt', 'content appended to notes.txt file')